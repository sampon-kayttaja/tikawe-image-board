# image-board

- The user can create an account and sign in to the app
- The user can make a new post that can include images
- The user can see their own posts and the posts of others
- The user can make comments that include photos on other posts
- The user can search for posts or users
- The user can like posts
- Every user has their own profile page that includes all their posts and some data about them




Testing the app:

- open the tikawe-image-board directory in terminal with "cd tikawe-image-board"
- then run the following commands
    "python3 -m venv venv"
    "source venv/bin/activate"
    "pip install flask"
    "flask run"
- now the app is running and you can open it in browser
- when you're done with testing run the command "deactivate"

Thank you for testing my app!